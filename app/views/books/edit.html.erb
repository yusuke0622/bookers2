 <div class="mb-2">
     <h2>Editing Book</h2>
 </div>
 
 <div>
  <% if @book.errors.any? %>
   <%= @book.errors.count %>errors prohibited this obj from bening saved:
   <ul>
    <%= @book.errors.full_messages.each do |message| %>
     <li><%= message %></li>
    <% end %>
   </ul>
   <% end %>
 </div>
 
 <div>
     <div>
         <%= form_with model: @book  do |f| %>
            <div>
             <%= f.label :Title %><br>
             <%= f.text_field :title %>
            </div>
            <div>
             <%= f.label :Opinion %><br>
             <%= f.text_area :body %>
            </div>
            <div>
             <%= f.submit 'Update Book' %>
            </div>
         <% end %>
         <%= link_to 'Show', book_path(@book.id) %>
         <%= link_to 'Back', :back %>
     </div>
 </div>
 